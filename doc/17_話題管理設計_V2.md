# 17_話題管理設計_V2.md

# 婚活AIトレーナー — TopicManager V2

Version: 2.0 (Phase 2)

---

# 1. Topic一覧

会話全体の話題を **Enum** で管理する。OpenAI に話題選択を委ねず、アプリ側で決定する。

| Enum | ラベル | 概要 |
| --- | --- | --- |
| `SELF_INTRODUCTION` | 自己紹介 | 初回・自己紹介フェーズ |
| `WORK` | 仕事 | 職業・やりがい |
| `COMMUTE` | 通勤・働き方 | 通勤・リモート等 |
| `HOLIDAY` | 休日 | 休日の過ごし方 |
| `HOBBY` | 趣味 | 趣味の深掘り |
| `FOOD` | 食べ物 | 食の好み |
| `TRAVEL` | 旅行 | 旅行の話 |
| `FAMILY` | 家族 | 家族観 |
| `VALUES` | 価値観 | 大切にしていること |
| `MARRIAGE` | 結婚観 | 結婚への考え |
| `LOVE` | 恋愛観 | 恋愛観（終盤寄り） |
| `FUTURE` | 将来 | 将来の展望 |
| `LIFESTYLE` | ライフスタイル | 生活リズム |
| `PET` | ペット | ペット・動物 |
| `OTHER` | その他 | フォールバック |

```mermaid
classDiagram
    class Topic {
        <<enumeration>>
        SELF_INTRODUCTION
        WORK
        COMMUTE
        HOLIDAY
        HOBBY
        FOOD
        TRAVEL
        FAMILY
        VALUES
        MARRIAGE
        LOVE
        FUTURE
        LIFESTYLE
        PET
        OTHER
    }
```

---

# 2. TopicState

セッションごとに保持する話題状態。

```typescript
interface TopicState {
  current: Topic;       // 現在の話題
  previous: Topic[];  // 直近の話題履歴（変更順）
  completed: Topic[]; // 完了済み話題
  turnCount: number;  // 現在話題でのターン数
  depth: number;      // 現在話題の深掘り度（1始まり）
}
```

```mermaid
stateDiagram-v2
    [*] --> SELF_INTRODUCTION: create()
    SELF_INTRODUCTION --> WORK: change() / update()
    WORK --> HOBBY: change() / update()
    HOBBY --> VALUES: change() / update()
    note right of WORK
        turnCount / depth 増加
        minTurns 満たすまで離脱不可
    end note
```

---

# 3. TopicRule

話題ごとの制約を `TopicRuleConfig.ts` に分離する。

| 話題 | minTurns | minTrust | minGlobalTurn | maxDepth |
| --- | --- | --- | --- | --- |
| WORK | 3 | — | — | 4 |
| HOBBY | 2 | — | — | 4 |
| LOVE | 2 | 55 | 8（終盤のみ） | 3 |
| MARRIAGE | 2 | 60 | — | 3 |
| VALUES | 3 | 45 | — | 4 |
| その他 | 1〜3 | 状況により | — | 2〜4 |

**主要メソッド**

| メソッド | 責務 |
| --- | --- |
| `canLeaveCurrentTopic()` | 最低ターンを満たしているか |
| `canSelectTopic()` | 信頼度・終盤条件を満たすか |
| `shouldCompleteCurrent()` | 深さ上限または十分なターンで完了 |
| `filterSelectableTopics()` | 候補リストをルールで絞り込み |

```mermaid
flowchart TD
    A[TopicRule] --> B[TOPIC_RULE_CONFIG]
    B --> C{canLeaveCurrentTopic?}
    C -->|No| D[現話題維持]
    C -->|Yes| E{canSelectTopic?}
    E -->|No| F[候補除外]
    E -->|Yes| G[選択可能]
```

---

# 4. TopicSelector

次の話題を決定する。入力はルールベースのみ（LLM 不使用）。

**入力**

| フィールド | 型 |
| --- | --- |
| `aiState` | `AIState` |
| `conversationHistory` | `ConversationHistoryMessage[]` |
| `topicState` | `TopicState` |
| `hiddenGoal` | `HiddenGoal` |

**出力**: `Topic`

**選択優先順位**

1. 現話題の `minTurns` 未達 → 現話題維持
2. HiddenGoal に対応する Topic（ルール通過時）
3. 未完了 Topic からランダム選択
4. 関心・楽しさが高い場合は現話題継続
5. フォールバック（完了済み含む）

```mermaid
flowchart LR
    subgraph Input
        AI[AIState]
        TH[TopicHistory]
        TS[TopicState]
        HG[HiddenGoal]
    end
    subgraph TopicSelector
        SEL[select / pickNextCandidate]
    end
    subgraph Output
        T[Topic]
    end
    AI --> SEL
    TH --> SEL
    TS --> SEL
    HG --> SEL
    SEL --> T
```

---

# 5. HiddenGoal連携

`HIDDEN_GOAL_TO_TOPIC` で HiddenGoal → 優先 Topic をマッピングする。

| HiddenGoal | 優先 Topic |
| --- | --- |
| JOB | WORK |
| HOLIDAY | HOLIDAY |
| VALUE | VALUES |
| MARRIAGE | MARRIAGE |
| FAMILY | FAMILY |
| HOBBY | HOBBY |
| FOOD | FOOD |
| TRAVEL | TRAVEL |
| FUTURE | FUTURE |
| LOVE | LOVE |

TopicSelector は候補選定時に Goal Topic を最優先で検討する（`TopicRule.canSelectTopic` を通過した場合のみ）。

```mermaid
flowchart TD
    HG[HiddenGoal] --> MAP[HIDDEN_GOAL_TO_TOPIC]
    MAP --> GT[Goal Topic]
    GT --> TR[TopicRule.canSelectTopic]
    TR -->|OK| SEL[TopicSelector が採用]
    TR -->|NG| FB[他候補へフォールバック]
```

---

# 6. PromptBuilder連携

OpenAI へ渡すプロンプトに以下を埋め込む（話題選択は行わない）。

| プレースホルダ | 内容 |
| --- | --- |
| `topic_instruction` | 現在話題 + 深さ + 深掘り指示 |
| `topic_depth` | `TopicState.depth` |
| `topic_next_hint` | `TOPIC_NEXT_HINTS[current]` |

```mermaid
sequenceDiagram
    participant TM as TopicManager
    participant PB as PromptBuilder
    participant OAI as OpenAI

    TM->>PB: topicState
    PB->>PB: getTopicInstruction(current, depth)
    PB->>PB: getTopicNextHint(current)
    PB->>OAI: system prompt（話題は確定済み）
```

---

# 7. AIState連携

Topic 更新は **AIState 更新後** に実行する。`AIStateManager` は Topic 選択を行わない。

| タイミング | 処理 |
| --- | --- |
| プロンプト構築前 | `applyTopicToConversation()` で `conversation.currentTopic` を同期 |
| OpenAI 応答後 | `AIStateManager.update()`（StateCalculator） |
| その直後 | `TopicManager.update()` |
| Topic 更新後 | 再度 `applyTopicToConversation()` |

```mermaid
sequenceDiagram
    participant User
    participant CA as ConversationAI
    participant ASM as AIStateManager
    participant TM as TopicManager
    participant PB as PromptBuilder
    participant OAI as OpenAI

    User->>CA: メッセージ
    CA->>ASM: get / create
    CA->>TM: getState / create
    CA->>ASM: applyTopicToConversation
    CA->>PB: aiState + topicState
    PB->>OAI: chat
    OAI-->>CA: reply
    CA->>ASM: update (StateCalculator)
    CA->>TM: update (TopicRule → TopicSelector)
    CA->>ASM: applyTopicToConversation
    CA-->>User: reply + debugTopic (dev)
```

---

# 8. 将来 MemoryManager と連携する方法

今回は未実装。将来の拡張ポイント:

| 連携先 | 想定用途 |
| --- | --- |
| `MemoryManager` | ユーザー発言から話題キーワード抽出 → `completed` 自動マーク |
| `ConversationDirector` | 全体の会話フェーズと Topic 遷移の統合制御 |
| LLM 判定 | 話題完了・感情分析の補助（現状はルールのみ） |

```mermaid
flowchart TB
  subgraph Future
    MM[MemoryManager]
    CD[ConversationDirector]
    LLM[LLM Topic Judge]
  end
  TM[TopicManager V2]
  MM -.->|discussed hints| TM
  CD -.->|phase override| TM
  LLM -.->|optional signal| TR[TopicRule]
  TM --> TR
  TM --> TS[TopicSelector]
```

**推奨インターフェース（案）**

```typescript
interface TopicMemorySignal {
  topic: Topic;
  confidence: number;
  source: "keyword" | "llm";
}
// TopicManager.update() の input に optional で追加
```

---

# 9. クラス図

```mermaid
classDiagram
    class TopicManager {
        -states: Map~string, TopicState~
        -topicRule: TopicRule
        -topicSelector: TopicSelector
        +create(sessionId)
        +getCurrent(sessionId)
        +change(sessionId, topic)
        +complete(sessionId, topic?)
        +increaseDepth(sessionId)
        +update(sessionId, input)
        +reset(sessionId)
    }

    class TopicRule {
        +getConfig(topic)
        +canLeaveCurrentTopic(state)
        +canSelectTopic(topic, context)
        +shouldCompleteCurrent(state)
        +filterSelectableTopics(candidates, context)
    }

    class TopicSelector {
        -topicRule: TopicRule
        +select(input)
        +pickNextCandidate(input)
    }

    class TopicRuleConfig {
        <<config file>>
        minTurns
        minTrust?
        minGlobalTurn?
        maxDepth?
    }

    class TopicState {
        current: Topic
        previous: Topic[]
        completed: Topic[]
        turnCount: number
        depth: number
    }

    TopicManager --> TopicRule
    TopicManager --> TopicSelector
    TopicRule --> TopicRuleConfig
    TopicSelector --> TopicRule
    TopicManager --> TopicState
    TopicSelector ..> TopicState : reads
    TopicSelector ..> AIState : reads
    TopicSelector ..> HiddenGoal : reads
```

---

# 10. シーケンス図（1ターン）

```mermaid
sequenceDiagram
    autonumber
    actor U as ユーザー
    participant CP as ConversationPage
    participant API as /api/message
    participant CA as ConversationAI
    participant ASM as AIStateManager
    participant TM as TopicManager
    participant TR as TopicRule
    participant TS as TopicSelector
    participant PB as PromptBuilder
    participant OAI as OpenAI

    U->>CP: 送信
    CP->>API: POST message
    API->>CA: reply()

    CA->>ASM: get / create AIState
    CA->>TM: getState / create TopicState
    CA->>ASM: applyTopicToConversation

    CA->>PB: buildConversationMessages(topicState)
    PB->>OAI: messages
    OAI-->>CA: assistant reply

    CA->>ASM: update (relationship / emotion)
    CA->>TM: update(sessionId, input)

    TM->>TM: turnCount++, depth++
    TM->>TR: shouldCompleteCurrent?
    alt 完了条件
        TM->>TM: complete(current)
    end
    TM->>TR: canLeaveCurrentTopic?
    alt 離脱可能
        TM->>TS: select(input)
        TS->>TR: canSelectTopic (各候補)
        TS-->>TM: next Topic
        TM->>TM: change(next)
    end
    TM->>TS: pickNextCandidate (debug用)
    TM-->>CA: TopicUpdateResult

    CA->>ASM: applyTopicToConversation
    CA-->>API: reply + debugTopic (dev)
    API-->>CP: MessageResponse
    CP->>CP: TopicDebugPanel 表示 (dev)
```

---

# MVP互換

| MVP API | V2 対応 |
| --- | --- |
| `getCurrentTopic()` | `getCurrent()` のエイリアス |
| `changeTopic()` | `change()` + `TopicHistory` 変換 |
| `getTopicHistory()` | `TopicState` → `TopicHistory` |
| `markDiscussed()` | `complete()` |
| `canAsk()` | `TopicRule.getMinTurns` 参照 |
| `incrementTurnCount()` | `update()` 内で統合（互換用に残存） |
